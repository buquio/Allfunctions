function getFirstAndLast(array) {
  let newArray =[];

    const first = array[0]
    const last = array[array.length-1]
newArray.push(first)
newArray.push(last)
}
    // return newArray
  
    console.log(getFirstAndLast([34, 56,8]))

